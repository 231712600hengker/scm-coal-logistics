@extends('layouts.app')
@section('title','Sales Order Detail')
@section('content')
<div class="rounded-xl border bg-white p-6 shadow-sm"><div class="grid gap-4 md:grid-cols-2"><div><p class="text-sm text-gray-500">SO Number</p><p class="font-semibold">{{ $salesOrder->so_number }}</p></div><div><p class="text-sm text-gray-500">Customer</p><p class="font-semibold">{{ $salesOrder->customer->name ?? '-' }}</p></div><div><p class="text-sm text-gray-500">Coal Product</p><p class="font-semibold">{{ $salesOrder->coalProduct->name ?? '-' }}</p></div><div><p class="text-sm text-gray-500">Order Date</p><p class="font-semibold">{{ $salesOrder->order_date }}</p></div><div><p class="text-sm text-gray-500">Quantity</p><p class="font-semibold">{{ number_format($salesOrder->quantity,2) }}</p></div><div><p class="text-sm text-gray-500">Price Per Ton</p><p class="font-semibold">{{ number_format($salesOrder->price_per_ton,2) }}</p></div><div><p class="text-sm text-gray-500">Total Amount</p><p class="font-semibold">{{ number_format($salesOrder->total_amount,2) }}</p></div><div><p class="text-sm text-gray-500">Status</p><p class="font-semibold capitalize">{{ $salesOrder->status }}</p></div><div><p class="text-sm text-gray-500">Shipment</p><p class="font-semibold">{{ $salesOrder->shipment->shipment_number ?? '-' }}</p></div></div><div class="mt-6 flex gap-2"><a href="{{ route('sales-orders.edit',$salesOrder) }}" class="rounded bg-gray-900 px-4 py-2 text-white">Edit</a><a href="{{ route('sales-orders.index') }}" class="rounded border px-4 py-2">Back</a></div></div>
@endsection
